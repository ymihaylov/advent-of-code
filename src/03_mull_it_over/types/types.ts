export type MemoryData = string;
export type MultiplyPair = [number, number];

export enum ParseMode {
    EnableCheck = "enableCheck",
    NoEnableCheck = "noEnableCheck"
}

export enum MulState {
    Do = "do",
    Dont = "don't"
}
